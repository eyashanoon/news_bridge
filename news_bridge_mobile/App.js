/**
 * App.js is NOT the entry point for this project.
 * The entry point is expo-router/entry (see package.json "main").
 * 
 * Routing is handled by the app/ directory using file-based routing:
 *   - app/_layout.tsx (root layout)
 *   - app/index.tsx (home screen)
 * 
 * This file is kept for reference only and is not used at runtime.
 */
export { default } from "./app/index";