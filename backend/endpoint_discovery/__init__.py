"""
endpoint_discovery — Listing endpoint discovery using the page_classifier ML model.

Package layout:
    page_classifier/     — XLM-RoBERTa page classifier (listing / content / other)
    listing_discoverer.py — BFS crawler with URL pattern grouping
    service.py           — FastAPI application
"""
